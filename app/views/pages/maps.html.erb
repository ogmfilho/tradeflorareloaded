<% provide(:title, 'Todas as áreas') %>
<h1>Mapa de Todas as Áreas</h1>

<p><%= link_to "Voltar para Áreas Disponíveis", areas_path, class: "br-button primary" %>

<div id="map"
      style="width: 100%; height: 800px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

<!-- MELHORAR PQ NAO ESTÁ DANDO PRA PASSAR PRA SHOW DIREITO SEM AS COORDENADAS DO POLIGONO NAS SEEDS -->